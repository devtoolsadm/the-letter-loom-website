// Copia este fichero como config.local.js y rellena los valores de dev.
// config.local.js está en .gitignore — nunca se commitea.

export const WORKER_BASE        = ''   // docs/secrets-and-vars.md → Worker URL → Dev
export const SUPABASE_URL       = ''   // docs/secrets-and-vars.md → Supabase URL → Dev
export const SUPABASE_ANON_KEY  = ''   // docs/secrets-and-vars.md → Anon key → Dev
export const TURNSTILE_SITE_KEY = ''   // docs/secrets-and-vars.md → Turnstile site key → Dev
