const LANDING_TEXTS = {
  es: {
    pageTitle: "The Letter Loom",
    metaDescription: "The Letter Loom, el juego de letras que te dejará sin palabras.",
    slogan1: "El juego de letras",
    slogan2: "que te dejará sin palabras",
    slogan1: "El juego de letras",
    slogan2: "que te dejará sin palabras",
    buyCta: "Quiero el juego",
    howCta: "Cómo se juega",
    buyGameCta: "Quiero el juego",
    emailLabel: "Email",
    buyModalTitle: "Demasiado pronto para sacar la cartera",
    buyModalLine1: "The Letter Loom aún está en fase prototipo.",
    buyModalLine2: "Si quieres, te avisamos cuando haya novedades.",
    buyModalCta: "Sí, quiero enterarme",
    buyModalSecondary: "Seguir explorando",
    guideModalTitle: "Cómo se juega",
    guideModalSubtitle: "Rápido de aprender. Bastante más difícil de olvidar.",
    guideModalIntro:
      "The Letter Loom es un juego de letras en el que toca pensar rápido y jugar con picardía. El objetivo no es solo formar palabras, sino sacar la mayor puntuación posible combinando el valor de las letras con cartas de estrategia para mejorar tu jugada o arruinar la de los demás. Y todo, claro, a contrarreloj. Aquí no gana quien encuentra la palabra más larga, gana quien puntúa mejor.",
    guideStep1Title: "Preparad la baza",
    guideStep1Body:
      "Pon 5 letras boca arriba en el centro. Cada jugador recibe 3 letras y 2 cartas de estrategia.",
    guideStep2Title: "Jugad con cabeza",
    guideStep2Body:
      "Primero va la fase de estrategia: cambias letras, robas, te proteges o atacas.",
    guideStep2Note: "Empieza el juego sucio: mejorar tu jugada está bien, fastidiar a los otros... también!",
    guideStep3Title: "Forma la palabra",
    guideStep3Body:
      "Ahora toca correr: usa tus letras y las del centro para montar la mejor palabra posible antes de que se acabe el tiempo.",
    guideStep3Note: "Recuerda! No gana la palabra más larga, gana la que mejor puntúa.",
    guideStep4Title: "Puntúa y sigue",
    guideStep4Body:
      "Enseñad las palabras, sumad puntos y pasad a la siguiente ronda. Repetid hasta llegar a la meta de bazas o de puntos.",
    guideStep4Note: "Letras, estrategia, ritmo y piques divertidos.",
    guideManualTitle: "¿Quieres conocer The Letter Loom a fondo?",
    guideManualBody:
      "Aquí tienes el manual completo, con todas las reglas, giros y cartas para cambiar la partida a tu favor o fastidiar a tus rivales.",
    guideManualCta: "Abrir manual",
    hero1Word1: "palabras",
    hero1Word2: "estrategia",
    hero1Word3: "pique",
    hero1Word4: "diversión",
    hero2Word1: "cartas de ataque",
    hero2Word2: "cartas de defensa",
    hero2Word3: "cartas colaborativas",
    hero2Word4: "estrategia y pique",
    hero3Word1: "app móvil",
    hero3Word2: "cronómetros",
    hero3Word3: "validador de palabras",
    hero3Word4: "compartir récords",
    hero4Word1: "síguelo",
    hero4Word2: "compártelo",
    hero4Word3: "últimas noticias",
    hero4Word4: "redes sociales",
    newsTitle: "Últimas noticias",
    newsTitle: "Últimas noticias",
    legalPrivacy: "Privacidad",
    legalCookies: "Cookies",
    legalNotice: "Aviso legal",
    legalDrawerClose: "Cerrar",
  },
  en: {
    pageTitle: "The Letter Loom",
    metaDescription: "The Letter Loom, the letter game that will leave you speechless.",
    slogan1: "The letter game",
    slogan2: "that will leave you speechless",
    slogan1: "The letter game",
    slogan2: "that will leave you speechless",
    buyCta: "I want the game",
    howCta: "How to play",
    buyGameCta: "I want the game",
    emailLabel: "Email",
    buyModalTitle: "Too early to reach for your wallet",
    buyModalLine1: "The Letter Loom is still in its prototype stage.",
    buyModalLine2: "If you want, we can let you know when there are updates.",
    buyModalCta: "Yes, keep me posted.",
    buyModalSecondary: "Keep exploring",
    guideModalTitle: "How to play",
    guideModalSubtitle: "Quick to learn. Much harder to put down.",
    guideModalIntro:
      "The Letter Loom is a word game where you need to think fast and play with a bit of cheek. The goal is not just to build words, but to squeeze out the highest score by combining letter values with strategy cards that improve your play or ruin everyone else's. And yes, all of it happens against the clock. The longest word does not win here: the best-scoring one does.",
    guideStep1Title: "Set up the trick",
    guideStep1Body:
      "Place 5 letters face up in the center. Each player gets 3 letters and 3 strategy cards.",
    guideStep2Title: "Play smart first",
    guideStep2Body:
      "Strategy comes first: swap letters, draw, protect yourself or change the score. Then race to build your word.",
    guideStep2Note: "This is where the fun starts: helping yourself is fair, making life harder for the others too.",
    guideStep3Title: "Build the word",
    guideStep3Body:
      "Now it is time to race: use your letters and the central ones to build the best word you can before time runs out.",
    guideStep3Note: "The longest word does not win here: the best-scoring one does.",
    guideStep4Title: "Score and keep going",
    guideStep4Body:
      "Reveal your words, add points, apply effects and pass the deal. Repeat until you hit the rounds or score goal.",
    guideStep4Note: "Letters, strategy, tempo and just enough friendly chaos.",
    guideManualTitle: "Want the full manual?",
    guideManualBody:
      "Here is the full manual, with all the rules, twists and cards to swing the game your way or make life harder for your rivals.",
    guideManualCta: "Open manual",
    hero1Word1: "words",
    hero1Word2: "strategy",
    hero1Word3: "rivalry",
    hero1Word4: "fun",
    hero2Word1: "attack cards",
    hero2Word2: "defense cards",
    hero2Word3: "collaborative cards",
    hero2Word4: "strategy and rivalry",
    hero3Word1: "mobile app",
    hero3Word2: "timers",
    hero3Word3: "word validator",
    hero3Word4: "share records",
    hero4Word1: "follow it",
    hero4Word2: "share it",
    hero4Word3: "latest news",
    hero4Word4: "social media",
    newsTitle: "Latest news",
    newsTitle: "Latest news",
    legalPrivacy: "Privacy",
    legalCookies: "Cookies",
    legalNotice: "Legal notice",
    legalDrawerClose: "Close",
  },
};

const LEGAL_CONTENT = {
  es: {
    privacy: {
      title: "Política de privacidad",
      body: [
        "Esta política se aplica al sitio web y al proyecto <strong>The Letter Loom</strong>, actualmente en fase de prototipo y validación de interés.",
        "El responsable del tratamiento es el titular del proyecto, como persona física. Si te apuntas a la lista de interés, contactas por correo o escribes a través de redes sociales, los datos que facilites se utilizarán para gestionar tu inscripción, responderte y enviarte información sobre The Letter Loom, su prototipo, su evolución, posibles pruebas, lanzamiento o campaña y, en su caso, novedades directamente relacionadas con este juego o con proyectos afines del mismo responsable dentro del ámbito de los juegos de mesa, letras o propuestas similares.",
        "La base jurídica del tratamiento, cuando te inscribes voluntariamente en una lista o formulario, es tu consentimiento. Puedes retirarlo en cualquier momento.",
        "Para la gestión de formularios o envíos informativos podrán utilizarse proveedores externos, como plataformas de email o automatización, que actuarán como encargados del tratamiento conforme a sus propias condiciones de servicio.",
        "No se cederán tus datos a terceros ajenos a la prestación de estos servicios salvo obligación legal. Las plataformas externas enlazadas desde este sitio, como Instagram, TikTok, X o servicios de correo, aplican además sus propias políticas.",
        "Puedes ejercer tus derechos de acceso, rectificación, supresión y demás derechos aplicables escribiendo a <strong>info@theletterloom.com</strong>."
      ],
    },
    cookies: {
      title: "Política de cookies",
      body: [
        "Esta página no está orientada actualmente a la venta ni al perfilado comercial de usuarios.",
        "En la versión actual se pretende limitar el uso de cookies a las estrictamente técnicas o necesarias para el funcionamiento básico del sitio. Si más adelante se incorporan analítica, medición o servicios de terceros que requieran consentimiento, esta política se actualizará.",
        "Las plataformas externas enlazadas desde esta landing, como redes sociales o futuras páginas de reserva, pueden aplicar sus propias cookies cuando navegues fuera de este sitio."
      ],
    },
    legal: {
      title: "Aviso legal",
      body: [
        "Este sitio web corresponde a <strong>The Letter Loom</strong>, un proyecto actualmente en fase de prototipo, difusión y validación de interés.",
        "El contenido mostrado tiene carácter informativo y promocional y puede cambiar durante el desarrollo del juego, sus pruebas, su futura campaña o su eventual comercialización.",
        "Para cualquier consulta relacionada con la web o el proyecto puedes escribir a <strong>info@theletterloom.com</strong>.",
        "Si el proyecto evoluciona a una actividad comercial formal, esta información legal se ampliará con los datos identificativos y regulatorios que correspondan."
      ],
    },
  },
  en: {
    privacy: {
      title: "Privacy policy",
      body: [
        "This policy applies to the website and the <strong>The Letter Loom</strong> project, currently in its prototype and interest-validation stage.",
        "The data controller is the project owner as an individual. If you join the interest list, contact us by email or send messages through social media, the data you provide may be used to manage your registration, reply to you and send you information about The Letter Loom, its prototype, development, possible playtests, launch or campaign and, where applicable, updates directly related to this game or to closely related projects by the same owner within the area of board games, letters or similar concepts.",
        "When you voluntarily subscribe to a list or form, the legal basis for processing is your consent. You may withdraw that consent at any time.",
        "External providers, such as email or automation platforms, may be used to manage forms or informational mailings, acting as data processors under their own service terms.",
        "Your data will not be disclosed to unrelated third parties unless required by law. External platforms linked from this site, such as Instagram, TikTok, X or email services, also apply their own policies.",
        "You can exercise your rights of access, rectification, erasure and other applicable rights by writing to <strong>info@theletterloom.com</strong>."
      ],
    },
    cookies: {
      title: "Cookies policy",
      body: [
        "This page is not currently focused on direct sales or commercial user profiling.",
        "At this stage, cookies are intended to be limited to strictly technical or necessary ones for the basic operation of the site. If analytics, tracking or third-party services requiring consent are added later, this policy will be updated.",
        "External platforms linked from this landing page, such as social media or future reservation pages, may apply their own cookies once you leave this site."
      ],
    },
    legal: {
      title: "Legal notice",
      body: [
        "This website relates to <strong>The Letter Loom</strong>, a project currently in its prototype, promotion and interest-validation phase.",
        "The content displayed is informational and promotional in nature and may change as the game, its playtests, future campaign or possible commercialization evolve.",
        "For any questions about the website or the project, you can write to <strong>info@theletterloom.com</strong>.",
        "If the project later becomes a formal commercial activity, this legal information will be expanded with the applicable identification and regulatory details."
      ],
    },
  },
};

const LEGAL_HASH_TO_KEY = {
  "#privacidad": "privacy",
  "#cookies": "cookies",
  "#aviso-legal": "legal",
};

const LEGAL_KEY_TO_HASH = {
  privacy: "#privacidad",
  cookies: "#cookies",
  legal: "#aviso-legal",
};

const BUY_HASHES = new Set(["#comprar", "#buy"]);
const NEWSLETTER_HASHES = new Set(["#newsletter"]);
const APP_HASHES = new Set(["#app"]);
const NEWSLETTER_URL =
  "https://1d9de069.sibforms.com/serve/MUIFAAIrDqstI-hPr3cgdMqYPGzym5zvEY3i8uwQPTJbK75JnRzSQpAjoV379OsBAtuQI7OM-iY-OsTrPckLKEQAIcsG0bS2N260sQBQdkD8IqcwdOu2nRCx59XomZBBC7TRcS70KpZOWlc29-gSdzJk92tjv-BgGsfIlRPhdYy-de9tL6m1CPmMJJ8KUmmMomlfRilmknzBNod6Jw==";

const STORAGE_KEY = "letterloom_landing_lang";
const SLIDE_INTERVAL_MS = 9000;
const MAX_PARALLAX_X = 10;
const MAX_PARALLAX_Y = 8;

function normalizeLang(lang) {
  if (!lang) return null;
  const clean = String(lang).toLowerCase();
  if (LANDING_TEXTS[clean]) return clean;
  const short = clean.split("-")[0];
  return LANDING_TEXTS[short] ? short : null;
}

function detectLanguage() {
  try {
    const stored = normalizeLang(localStorage.getItem(STORAGE_KEY));
    if (stored) return stored;
  } catch (err) {
    console.warn("Language storage unavailable", err);
  }

  const candidates = [
    ...(Array.isArray(navigator.languages) ? navigator.languages : []),
    navigator.language,
  ];
  return candidates.map(normalizeLang).find(Boolean) || "es";
}

let currentLang = detectLanguage();
let activeSlide = 0;
let slideTimer = null;
let openLegalKey = null;
let feedwindRawBuffer = null;

document.addEventListener("feedwind-raw", (e) => { feedwindRawBuffer = e.detail; });

function openBuyModal() {
  const modal = document.getElementById("buyModal");
  if (!modal) return;
  modal.hidden = false;
  modal.setAttribute("aria-hidden", "false");
  document.body.classList.add("reduced-motion");
}

function closeBuyModal(clearHash = true) {
  const modal = document.getElementById("buyModal");
  if (!modal) return;
  modal.hidden = true;
  modal.setAttribute("aria-hidden", "true");
  document.body.classList.remove("reduced-motion");
  if (clearHash && BUY_HASHES.has(window.location.hash)) {
    const url = `${window.location.pathname}${window.location.search}`;
    window.history.replaceState(null, "", url);
  }
}

function openGuideModal() {
  const modal = document.getElementById("guideModal");
  if (!modal) return;
  modal.hidden = false;
  modal.setAttribute("aria-hidden", "false");
}

function closeGuideModal() {
  const modal = document.getElementById("guideModal");
  if (!modal) return;
  modal.hidden = true;
  modal.setAttribute("aria-hidden", "true");
}

function syncBuyModalWithHash() {
  if (BUY_HASHES.has(window.location.hash)) {
    openBuyModal();
    return;
  }
  closeBuyModal(false);
}

function openNewsletter({ clearHash = true, newWindow = false } = {}) {
  if (newWindow) {
    window.open(NEWSLETTER_URL, "_blank", "noopener");
  } else {
    window.location.href = NEWSLETTER_URL;
  }

  if (clearHash && NEWSLETTER_HASHES.has(window.location.hash)) {
    const url = `${window.location.pathname}${window.location.search}`;
    window.history.replaceState(null, "", url);
  }
}

function syncNewsletterHash() {
  if (NEWSLETTER_HASHES.has(window.location.hash)) {
    openNewsletter();
  }
}

function openAppFromHash() {
  window.location.href = "../app/";
}

function syncAppHash() {
  if (APP_HASHES.has(window.location.hash)) {
    openAppFromHash();
  }
}

function populateLangSelect() {
  const select = document.getElementById("langSelect");
  if (!select) return;

  select.innerHTML = "";
  Object.keys(LANDING_TEXTS).forEach((lang) => {
    const option = document.createElement("option");
    option.value = lang;
    option.textContent = lang === "es" ? "ES" : "EN";
    select.appendChild(option);
  });

  select.value = currentLang;
  select.addEventListener("change", (event) => {
    setLanguage(event.target.value);
  });
}

function renderTextNodes() {
  const dict = LANDING_TEXTS[currentLang];
  document.documentElement.lang = currentLang;
  document.title = dict.pageTitle;

  const metaDescription = document.querySelector('meta[name="description"]');
  if (metaDescription) {
    metaDescription.setAttribute("content", dict.metaDescription);
  }

  document.querySelectorAll("[data-i18n]").forEach((node) => {
    const key = node.getAttribute("data-i18n");
    if (key === "slogan1" && dict.slogan1) {
      node.textContent = dict.slogan1;
    } else if (key === "slogan2" && dict.slogan2) {
      node.textContent = dict.slogan2;
    } else if (key && dict[key]) {
      node.textContent = dict[key];
    }
  });

  renderLegalLinks();
  renderLegalDrawer();
}

function renderLegalLinks() {
  const dict = LANDING_TEXTS[currentLang];
  document.querySelectorAll("[data-legal-open]").forEach((button) => {
    const key = button.getAttribute("data-legal-open");
    if (key === "privacy") button.textContent = dict.legalPrivacy;
    if (key === "cookies") button.textContent = dict.legalCookies;
    if (key === "legal") button.textContent = dict.legalNotice;
  });

  const closeButton = document.querySelector(".legal-close");
  if (closeButton) {
    closeButton.setAttribute("aria-label", dict.legalDrawerClose);
  }

  const yearNode = document.getElementById("legalYear");
  if (yearNode) {
    yearNode.textContent = String(new Date().getFullYear());
  }
}

function renderLegalDrawer() {
  const drawer = document.getElementById("legalDrawer");
  const title = document.getElementById("legalDrawerTitle");
  const body = document.getElementById("legalDrawerBody");
  if (!drawer || !title || !body) return;

  if (!openLegalKey) {
    drawer.hidden = true;
    drawer.setAttribute("aria-hidden", "true");
    return;
  }

  const section = LEGAL_CONTENT[currentLang]?.[openLegalKey];
  if (!section) return;

  title.textContent = section.title;
  body.innerHTML = section.body.map((paragraph) => `<p>${paragraph}</p>`).join("");
  drawer.hidden = false;
  drawer.setAttribute("aria-hidden", "false");
}

function openLegalDrawer(key) {
  openLegalKey = key;
  renderLegalDrawer();
}

function closeLegalDrawer(clearHash = true) {
  openLegalKey = null;
  renderLegalDrawer();
  if (clearHash && LEGAL_HASH_TO_KEY[window.location.hash]) {
    const url = `${window.location.pathname}${window.location.search}`;
    window.history.replaceState(null, "", url);
  }
}

function syncLegalDrawerWithHash() {
  const key = LEGAL_HASH_TO_KEY[window.location.hash];
  if (key) {
    openLegalDrawer(key);
    return;
  }
  closeLegalDrawer(false);
}

function initLegalDrawer() {
  document.querySelectorAll("[data-legal-open]").forEach((button) => {
    button.addEventListener("click", (event) => {
      const key = button.getAttribute("data-legal-open");
      if (!key) return;
      if (openLegalKey === key) {
        event.preventDefault();
        closeLegalDrawer();
        return;
      }
      const hash = LEGAL_KEY_TO_HASH[key];
      if (hash) {
        event.preventDefault();
        window.location.hash = hash;
      }
    });
  });

  document.querySelectorAll("[data-legal-close]").forEach((node) => {
    node.addEventListener("click", closeLegalDrawer);
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") {
      closeLegalDrawer();
    }
  });

  window.addEventListener("hashchange", syncLegalDrawerWithHash);
  syncLegalDrawerWithHash();
}

function initBuyModal() {
  document.querySelectorAll("[data-buy-open]").forEach((node) => {
    node.addEventListener("click", () => {
      const hash = currentLang === "en" ? "#buy" : "#comprar";
      window.location.hash = hash;
    });
  });

  document.querySelectorAll("[data-buy-close]").forEach((node) => {
    node.addEventListener("click", closeBuyModal);
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") {
      closeBuyModal();
    }
  });

  window.addEventListener("hashchange", syncBuyModalWithHash);
  syncBuyModalWithHash();
}

function initNewsletter() {
  document.querySelectorAll("[data-newsletter-open]").forEach((node) => {
    node.addEventListener("click", () => {
      openNewsletter({ clearHash: false, newWindow: true });
    });
  });

  window.addEventListener("hashchange", syncNewsletterHash);
  syncNewsletterHash();
}

function initAppHash() {
  window.addEventListener("hashchange", syncAppHash);
  syncAppHash();
}

function initGuideModal() {
  document.querySelectorAll("[data-guide-open]").forEach((node) => {
    node.addEventListener("click", openGuideModal);
  });

  document.querySelectorAll("[data-guide-close]").forEach((node) => {
    node.addEventListener("click", closeGuideModal);
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") {
      closeGuideModal();
    }
  });
}

function setLanguage(lang) {
  const normalized = normalizeLang(lang);
  if (!normalized || normalized === currentLang) return;

  currentLang = normalized;
  try {
    localStorage.setItem(STORAGE_KEY, currentLang);
  } catch (err) {
    console.warn("Language storage unavailable", err);
  }
  renderTextNodes();
}

function setSlide(index) {
  const slides = Array.from(document.querySelectorAll(".hero-slide"));
  const dots = Array.from(document.querySelectorAll(".hero-dot"));
  if (!slides.length) return;

  const normalized = ((index % slides.length) + slides.length) % slides.length;
  activeSlide = normalized;

  slides.forEach((slide, slideIndex) => {
    slide.classList.toggle("is-active", slideIndex === normalized);
  });
  dots.forEach((dot, dotIndex) => {
    dot.classList.toggle("is-active", dotIndex === normalized);
    dot.setAttribute("aria-pressed", dotIndex === normalized ? "true" : "false");
  });
}

function startCarousel() {
  const dots = Array.from(document.querySelectorAll(".hero-dot"));
  dots.forEach((dot) => {
    dot.addEventListener("click", () => {
      const target = Number(dot.getAttribute("data-slide-target"));
      setSlide(target);
      scheduleCarousel();
    });
  });

  scheduleCarousel();
}

function scheduleCarousel() {
  if (slideTimer) {
    window.clearInterval(slideTimer);
    slideTimer = null;
  }

  if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
    return;
  }

  slideTimer = window.setInterval(() => {
    setSlide(activeSlide + 1);
  }, SLIDE_INTERVAL_MS);
}

function initParallax() {
  const stage = document.getElementById("stage");
  const depthRoot = document.getElementById("heroDepth");
  if (!stage || !depthRoot) return;

  const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)");
  if (prefersReducedMotion.matches) {
    document.body.classList.add("reduced-motion");
    return;
  }

  const layered = Array.from(document.querySelectorAll("[data-depth]"));
  const setTransforms = (ratioX, ratioY) => {
    layered.forEach((node) => {
      const depth = Number(node.getAttribute("data-depth")) || 0;
      const moveX = (ratioX * depth * MAX_PARALLAX_X) / 20;
      const moveY = (ratioY * depth * MAX_PARALLAX_Y) / 20;
      node.style.setProperty("--tx", `${moveX.toFixed(2)}px`);
      node.style.setProperty("--ty", `${moveY.toFixed(2)}px`);
    });

    depthRoot.style.transform = `rotateX(${(-ratioY * 2.2).toFixed(2)}deg) rotateY(${(
      ratioX * 2.8
    ).toFixed(2)}deg)`;
  };

  stage.addEventListener("pointermove", (event) => {
    const rect = stage.getBoundingClientRect();
    const ratioX = (event.clientX - rect.left) / rect.width - 0.5;
    const ratioY = (event.clientY - rect.top) / rect.height - 0.5;
    setTransforms(ratioX, ratioY);
  });

  stage.addEventListener("pointerleave", () => {
    setTransforms(0, 0);
  });
}

const MONTHS = {
  es: ["ene","feb","mar","abr","may","jun","jul","ago","sep","oct","nov","dic"],
  en: ["jan","feb","mar","apr","may","jun","jul","aug","sep","oct","nov","dec"],
};

function formatFeedDate(raw, lang) {
  // raw format from FeedWind: "DD.MM.YYYY"
  const parts = raw.split(".");
  if (parts.length < 3) return raw;
  const day = parseInt(parts[0], 10);
  const month = parseInt(parts[1], 10) - 1;
  const year = parseInt(parts[2], 10);
  const months = MONTHS[lang] || MONTHS.es;
  const now = new Date();
  const tweetDate = new Date(year, month, day);
  const oneYearAgo = new Date(now.getFullYear() - 1, now.getMonth(), now.getDate());
  const aged = tweetDate < oneYearAgo;
  const de = lang === "en" ? " " : " de ";
  return `${day} ${months[month]}${aged ? `${de}${year}` : ""}`;
}

function escapeHtml(str) {
  return String(str)
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/"/g, "&quot;");
}

function renderFeedItems(items) {
  const list = document.querySelector(".hero-news-list");
  if (!list) return;
  list.innerHTML = items.map((item) => {
    const imgHtml = item.image
      ? `<img class="hero-news-img" src="${escapeHtml(item.image)}" alt="" loading="lazy" onerror="this.style.display='none'" />`
      : "";
    const formattedDate = item.date ? formatFeedDate(item.date, currentLang) : null;
    const dateHtml = formattedDate ? `<time class="hero-news-date">${escapeHtml(formattedDate)}</time>` : "";
    const titleTag = item.link
      ? `<a class="hero-news-title-inferred" href="${escapeHtml(item.link)}" target="_blank" rel="noopener">${escapeHtml(item.title)}</a>`
      : `<strong class="hero-news-title-inferred">${escapeHtml(item.title)}</strong>`;
    const headHtml = item.title
      ? `<div class="hero-news-head">${dateHtml}${titleTag}</div>`
      : dateHtml;
    const inner = `${imgHtml}${headHtml}<p class="hero-news-text">${item.textHtml}</p>`;
    return `<li class="hero-news-item">${inner}</li>`;
  }).join("");
}

function inferTitle(plainText) {
  // First sentence + any immediately following emoji/symbol chars (non-letter, non-digit)
  const m = plainText.match(/^(.{8,80}[.!?])(\s*[^\p{L}\p{N}\s!?.,:;\-"'«»¡¿(){}[\]]+)?/u);
  if (m) {
    const title = (m[1] + (m[2] || "")).trim();
    return { title, consumed: m[0].length };
  }
  const n = Math.min(60, plainText.length);
  return { title: plainText.slice(0, n).trimEnd() + (plainText.length > n ? "…" : ""), consumed: n };
}

// Walk html string consuming numChars of plain text, skipping tags
function stripHtmlPrefix(html, numChars) {
  let h = 0, p = 0;
  while (h < html.length && p < numChars) {
    if (html[h] === "<") {
      const end = html.indexOf(">", h);
      h = end < 0 ? html.length : end + 1;
    } else {
      const cp = html.codePointAt(h);
      const w = cp > 0xFFFF ? 2 : 1;
      h += w; p += w;
    }
  }
  while (h < html.length && html[h] !== "<" && /\s/.test(html[h])) h++;
  return html.slice(h);
}

function extractFromShadow(root) {
  const items = [];
  root.querySelectorAll(".fw-tw-feed-item").forEach((el) => {
    const descEl = el.querySelector(".fw-tw-feed-item-description");
    const plainText = descEl ? descEl.textContent.trim() : "";
    if (!plainText) return;
    const { title, consumed } = inferTitle(plainText);

    const imgEl = el.querySelector(".fw-tw-feed-item-image img");
    const image = imgEl?.getAttribute("data-fw-img-src") || null;

    const dateEl = el.querySelector(".fw-tw-feed-item-date");
    const date = dateEl ? dateEl.textContent.trim().split(/\s+/)[0] : null;
    const link = dateEl?.closest("a")?.href || el.querySelector(".fw-tw-feed-item-user-info > a")?.href || null;

    // Strip title from body and remove any links whose text is a URL (self-references)
    let bodyHtml = stripHtmlPrefix(descEl.innerHTML, consumed);
    if (link) {
      const tmp = new DOMParser().parseFromString(`<div>${bodyHtml}</div>`, "text/html");
      tmp.querySelectorAll("a").forEach((a) => {
        if (a.textContent.trim().match(/^https?:\/\//)) a.remove();
      });
      bodyHtml = tmp.querySelector("div").innerHTML.trim();
    }

    items.push({ title, textHtml: bodyHtml, image, link, date });
  });
  return items.slice(0, 8);
}

function parseFeedWindHtml(html) {
  return new DOMParser().parseFromString(html, "text/html");
}

function processFeedWindData(detail, aside) {
  try {
    const json = JSON.parse(detail.text);
    const html = json?.response?.widget;
    if (!html) return;
    const doc = parseFeedWindHtml(html);
    const items = extractFromShadow(doc.body);
    if (items.length) {
      const host = aside.querySelector(".fw-block");
      if (host) host.style.display = "none";
      renderFeedItems(items);
    }
  } catch (err) {
    console.error("[FeedWind] Error parseando respuesta:", err);
  }
}

function initFeedWind() {
  const aside = document.querySelector(".hero-news");
  if (!aside) return;

  if (feedwindRawBuffer) {
    processFeedWindData(feedwindRawBuffer, aside);
  } else {
    document.addEventListener("feedwind-raw", (e) => processFeedWindData(e.detail, aside), { once: true });
  }
}

document.addEventListener("DOMContentLoaded", () => {
  populateLangSelect();
  renderTextNodes();
  initLegalDrawer();
  initBuyModal();
  initNewsletter();
  initGuideModal();
  initAppHash();
  setSlide(0);
  startCarousel();
  initParallax();
  initFeedWind();
  initFeedWind();
});
